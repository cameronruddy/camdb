import os, sys
import color_scheme as clr

class display:
    directory_paths = []

    def __init__(self, directory_paths):
        self.directory_paths = directory_paths
    
    def display(self):
        for 
